import { PlaybookEditorScreen } from "../PlaybookEditorScreen";

export const metadata = { title: "Design a playbook · Interbellum Console" };

export default function NewPlaybookPage() {
  return <PlaybookEditorScreen />;
}
